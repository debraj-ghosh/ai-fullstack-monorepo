name: Deploy Production

on:

  workflow_dispatch:

jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

      - name: Placeholder

        run: |

          echo "Production deployment will be added later"